spring:
  profiles:
    active: logback-test
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ""
          jwk-set-uri: ""
accounts:
  service:
    url: http://localhost:8081
