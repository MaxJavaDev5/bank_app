apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.fullnameOverride }}
  labels:
    app: {{ .Values.fullnameOverride }}
spec:
  clusterIP: None
  selector:
    app: {{ .Values.fullnameOverride }}
  ports:
    - port: 5432
      targetPort: 5432
