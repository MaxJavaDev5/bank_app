apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cash-service.fullname" . }}
  labels:
    {{- include "cash-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "cash-service.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cash-service.labels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "cash-service.fullname" . }}-config
          env:
            - name: SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_CASHCLIENT_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "cash-service.fullname" . }}-secret
                  key: client-secret
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
