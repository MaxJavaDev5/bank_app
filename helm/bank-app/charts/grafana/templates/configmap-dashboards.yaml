apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-dashboards
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
data:
  {{- (.Files.Glob "config/dashboards/*.json").AsConfig | nindent 2 }}
